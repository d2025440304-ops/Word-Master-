在左栏统计数据和工具栏之间，新增一个"单词本"折叠区。
不新增页面，不改任何 JS 算法和数据结构。

## 单词本结构

折叠区标题行：
- 左侧文字"单词本"
- 右侧一个展开/收起箭头，默认收起
- 展开后最大高度 240px，超出滚动，不撑开左栏

顶部 tab 切换（两个，小尺寸）：
- "全部"：显示所有 level > 0 的词
- "已掌握"：只显示 level >= 6 的词

每行格式：
- 左侧：单词 + 空格 + 释义，单行截断超出部分
- 右侧：一个小圆点，level >= 6 显示绿色（var(--success)），否则显示 var(--text-tertiary) 浅灰色
- 字号 13px，行高 32px，hover 背景轻微高亮

## 数据来源

从 appData.words 遍历，level > 0 的词才显示。
需要从当前激活词库（getActiveWords()）找到对应的词义，用 word.m 字段。
按 level 从高到低排序，同 level 按字母排序。

## 约束
- 不改任何 JS 逻辑，只新增 DOM 和相关渲染函数
- 单词本数据在 updateStatsUI() 末尾调用一次 renderWordBook() 刷新
- 折叠状态存在内存变量里，不存 localStorage
- 改完截图给我看



# Role & Objective
你是一位顶尖的全栈架构师。现在我需要将一个精美的单文件原生前端登录卡片（包含 Email、Password、Username 输入项）接入 Supabase Auth 认证服务，并考虑最终部署在 Netlify 平台。请在保留原有高级 UI/UX、CSS 动画及 DOM 结构完全不变的前提下，为我编写健壮的 JavaScript 业务连接层。

# Tech Stack & Specs
1. 客户端 SDK：使用 Supabase CDN 引入最新版本的 `@supabase/supabase-js`。
2. 环境变量管理：由于项目最终部署在 Netlify，Supabase 的 `SUPABASE_URL` 和 `SUPABASE_ANON_KEY` 需要通过 Netlify 环境变量注入，或者在客户端安全初始化。
3. 核心业务逻辑：
   - 登录分支：调用 `supabase.auth.signInWithPassword`。
   - 注册分支：调用 `supabase.auth.signUp`。若为注册，需同时将用户输入的 `username` 写入 `options.data` 属性（以便存入 Supabase 的 metadata 中）。
   - 持久化：确保登录成功后，Supabase 会自动将 JWT 令牌持久化在 LocalStorage 中，并提供一个检测当前登录状态（`supabase.auth.getSession()`）的初始化逻辑，若已登录则直接跳转或提示。

# Code Refactoring & Robustness (防崩溃规范)
1. 必须使用 strict 模式及 async/await 语法，并用完美的 try...catch 块包裹所有网络请求。
2. 发生错误时，不要直接 throw，捕获 Supabase 返回的 `error.message`（例如密码错误、邮箱格式不正确），并优雅地调用前端现有的 `showToast(error.message)` 进行报错。
3. 在请求发起时，必须通过 `btn.disabled = true` 禁用提交按钮，请求结束后恢复，彻底防范用户快速连击造成的二次冗余请求。

请直接输出重构后的完整 <script> 逻辑和 Supabase 初始化配置代码。

# Role
你是一位精通前端 UI 开发与 Supabase 认证体系的全栈专家。

# Background & Objective
我刚刚设计了一个基于极简物理质感和流光艺术（Aura Glow）的登录/注册单文件 HTML 组件。现在我需要你帮我接入 Supabase Auth 服务的原生 SDK，实现真正的用户注册和登录功能，同时确保前端已有的所有精美 CSS 样式、DOM 节点 ID、以及流光背景完全不被破坏。

# Implementation Details
1. 请在 <head> 中通过 CDN 引入最新版的 Supabase JS SDK：
   https://cdn.jsdelivr.net/npm/@supabase/supabase-js
2. 在 <script> 标签顶部，编写 Supabase 的客户端初始化代码。从安全和 Netlify 部署的最佳实践出发，请预留出 `const SUPABASE_URL = ""` 和 `const SUPABASE_ANON_KEY = ""` 的常量位置，方便我填写。
3. 请重构 `executeAuth(e)` 函数：
   - 提取电子邮箱（#fieldEmail）、密码（#fieldPassword）以及昵称（#fieldNickname）的值。
   - 当 `currentViewState === 'login'` 时，调用 `supabase.auth.signInWithPassword`。
   - 当 `currentViewState === 'register'` 时，调用 `supabase.auth.signUp`。必须将用户输入的昵称（nickname）通过 `options: { data: { display_name: nickname } }` 结构一并传入，以便写入 Supabase 的用户元数据中。
4. 异常处理：
   - 使用完美的 try...catch 结构。如果请求返回 error，捕获其 message 并将其作为参数传给页面现有的 `notify(error.message)` 函数，优雅地在界面上弹窗告警。
5. 登录成功处理：
   - 登录成功后，利用 `notify('成功提示')` 告知用户，并预留一个页面跳转的逻辑（例如 `window.location.href = '/dashboard'`）。

请直接给出更新后的完整 JS 脚本部分，保持原有的交互逻辑平滑稳健。